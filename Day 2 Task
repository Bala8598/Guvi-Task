Write a blog on the difference between document & window object
Ans : Document => *Its a loaded inside the Window.
                  *Its a object of window property.
                  *Web pages
                  *Each HTML document that gets loaded into a window becomes a document object.
                  *The document contains the contents of the page. Using document object, JavaScript can modify, add and delete the HTML elements, attributes CSS styles in the page

 Window object => *Its the very first object that is loaded in the browser.
                  *Its the object of the Browser
                  *Individual tabs or Browser Window
                  *The window object represents a window/tab containing a DOM document where as document object is property of window object that points to the DOM document loaded in that window.

